name: docs
version: 0.0.0
author: David Marne <davemarne@gmail.com>

dependencies:
  prettify: ^0.2.1
  wui_builder:
    path: ../../

dev_dependencies:
  build_runner: ^0.7.9
  build_web_compilers: ^0.3.0
  livereload: ^0.4.0

transformers:
  - $dart2js

environment:
  sdk: ">=1.23.0 <2.0.0"

web:
  compiler:
    debug: dartdevc
